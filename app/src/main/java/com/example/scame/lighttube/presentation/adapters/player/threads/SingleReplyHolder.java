package com.example.scame.lighttube.presentation.adapters.player.threads;


import android.view.View;

import com.example.scame.lighttube.presentation.fragments.CommentActionListener;

public class SingleReplyHolder extends CommentsViewHolder {

    public SingleReplyHolder(View itemView, CommentActionListener actionListener, String identifier) {
        super(itemView, identifier, actionListener, null);
    }
}
